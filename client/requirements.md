## Packages
framer-motion | Required for premium scroll animations and page transitions
react-hook-form | Required for the contact form state management
@hookform/resolvers | Required to connect Zod schema to react-hook-form
lucide-react | Required for beautiful icons throughout the landing page

## Notes
Static images are imported directly from @assets alias as requested.
The contact form uses the provided POST /api/contacts endpoint.
